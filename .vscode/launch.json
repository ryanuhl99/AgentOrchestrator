{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to PlannerAgent Container",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickRemoteProcess}"
        }
    ]
}