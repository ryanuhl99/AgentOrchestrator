version: "3.9"

services:

  planneragent:
    build:
      context: ./PlannerAgent
      dockerfile: Dockerfile
    ports:
      - "5000:8080"

  researchagent:
    build:
      context: ./ResearchAgent
      dockerfile: Dockerfile
    ports:
      - "5001:8080"

  codeagent:
    build:
      context: ./CodeAgent
      dockerfile: Dockerfile
    ports:
      - "5002:8080"

  reviewagent:
    build:
      context: ./ReviewAgent
      dockerfile: Dockerfile
    ports:
      - "5003:8080"